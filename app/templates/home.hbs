{{#if session.isAuthenticated}}
  <div class='row'>
  {{#side-bar class='col-md-1'}}
    <ul class="nav nav-pills nav-stacked">
      <li class="nav-item">
        {{#link-to 'home.myFavorites' class='nav-link'}}
          Favorites
        {{/link-to}}
      </li>
      <li class="nav-item">
        {{#link-to 'home.myRecordCollection' class='nav-link'}}
          Collection
        {{/link-to}}
      </li>
    </ul>
  {{/side-bar}}
  <div class='col-md-11'>
    {{outlet}}
  </div>
  </div>
{{else}}
  <div class='row'>
    <div class='col-md-4 col-md-offset-4'>
      {{login-card id='main-login'}}
    </div>
  </div>
{{/if}}
